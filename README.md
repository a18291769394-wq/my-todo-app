# my-todo-app
A simple to-do list application with local storage functionality

## Java 运算符示例

新增 `src/OperatorDemo.java`，包含：
- 任务一：变量与算术运算符（+、-、*、/、%）示例
- 任务二：赋值运算符（=、+=、-=、*=、/=、%=）和比较运算符（>、<、>=、<=、==、!=）示例

### 运行方式

```bash
javac src/OperatorDemo.java
java -cp src OperatorDemo
```
